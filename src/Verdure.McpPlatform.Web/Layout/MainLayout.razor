@inherits LayoutComponentBase

<MudThemeProvider Theme="_theme" />
<MudPopoverProvider />
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>
    <MudAppBar Elevation="2">
        <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start" OnClick="@ToggleDrawer" />
        <MudText Typo="Typo.h5" Class="ml-3">Verdure MCP Platform</MudText>
        <MudSpacer />
        <AuthorizeView>
            <Authorized>
                <MudText Typo="Typo.body1" Class="mr-4">@context.User.Identity?.Name</MudText>
                <MudIconButton Icon="@Icons.Material.Filled.AccountCircle" Color="Color.Inherit" />
            </Authorized>
            <NotAuthorized>
                <MudButton Href="/login" Variant="Variant.Text" Color="Color.Inherit">Login</MudButton>
            </NotAuthorized>
        </AuthorizeView>
    </MudAppBar>
    
    <MudDrawer @bind-Open="_drawerOpen" Elevation="2" ClipMode="DrawerClipMode.Always">
        <NavMenu />
    </MudDrawer>
    
    <MudMainContent>
        <MudContainer MaxWidth="MaxWidth.ExtraLarge" Class="mt-4 mb-4">
            @Body
        </MudContainer>
    </MudMainContent>
</MudLayout>

@code {
    private bool _drawerOpen = true;
    private MudTheme _theme = new()
    {
        PaletteLight = new PaletteLight()
        {
            Primary = "#7c3aed", // Vibrant violet
            Secondary = "#a78bfa", // Light violet
            AppbarBackground = "#7c3aed",
            Background = "#faf5ff", // Very light violet tint
            Surface = "#ffffff",
            DrawerBackground = "#ffffff",
            DrawerText = "rgba(0,0,0, 0.87)",
            Success = "#10b981", // Green
            Info = "#6366f1", // Indigo
            Warning = "#f59e0b", // Amber
            Error = "#ef4444", // Red
            TextPrimary = "rgba(0,0,0, 0.87)",
            TextSecondary = "rgba(0,0,0, 0.60)"
        },
        PaletteDark = new PaletteDark()
        {
            Primary = "#a78bfa", // Light violet for dark mode
            Secondary = "#c4b5fd", // Lighter violet
            AppbarBackground = "#1f1733", // Dark violet
            Background = "#0f0817", // Very dark violet
            Surface = "#1f1733",
            DrawerBackground = "#1f1733",
            DrawerText = "rgba(255,255,255, 0.87)",
            Success = "#10b981",
            Info = "#818cf8",
            Warning = "#fbbf24",
            Error = "#f87171",
            TextPrimary = "rgba(255,255,255, 0.87)",
            TextSecondary = "rgba(255,255,255, 0.60)"
        },

    };

    private void ToggleDrawer()
    {
        _drawerOpen = !_drawerOpen;
    }
}
